<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/accent" /> <!-- Rojo (Definido en color.xml) -->
    <corners android:radius="0dp" /> <!-- Bordes rectos -->
</shape>
